apiVersion: v1
kind: Service
metadata:
  name: web-fe
  namespace: demo
  annotations:
    argocd.argoproj.io/sync-wave: "3"
spec:
  type: NodePort
  selector:
    app: web-fe
  ports:
  - port: 80
    targetPort: 80
