apiVersion: v1
kind: Service
metadata:
  name: web-be
  namespace: demo
  annotations:
    argocd.argoproj.io/sync-wave: "3"
spec:
  selector:
    app: web-be
  ports:
  - port: 8080
    targetPort: 80
